export type apiPhone = {
  is_valid: boolean;
  country: string;
};

export type apiCountry = {
  name: string;
  capital: string;
};

export type apiCity = {
  name: string;
  latitude: string;
  longitude: string;
  country: string;
};
